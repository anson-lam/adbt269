package game;

import city.cs.engine.*;
import org.jbox2d.common.Vec2;

public class GameWorld extends World {
/*
    private Jet jet;

    public GameWorld(){

        // make the ground
        Ground ground = new Ground(this);
        ground.setPosition(new Vec2(0, -10f));

        //Shape groundShape = new BoxShape(15, 2);
        //StaticBody ground = new StaticBody(this, groundShape);
        //ground.setPosition(new Vec2(0, -10f));

        ////VirusDestroyed destroyed = new VirusDestroyed(ground);
        ////ground.addCollisionListener(destroyed);


        // make walls
        Shape wallShape = new BoxShape(15, 30);

        StaticBody wall1 = new StaticBody(this, wallShape);
        wall1.setPosition(new Vec2(-30, 10));

        StaticBody wall2 = new StaticBody(this, wallShape);
        wall2.setPosition(new Vec2(25, 10));

        // make the jet
        jet = new Jet(this);
        jet.setPosition(new Vec2(-2, -5));

        InfectedWithVirus infected = new InfectedWithVirus(jet);
        jet.addCollisionListener(infected);

        CollectHealthPoint collect = new CollectHealthPoint(jet);
        jet.addCollisionListener(collect);

        //make the black hole
        BlackHole blackHole = new BlackHole(this);
        blackHole.setPosition(new Vec2(-2, 10));

        // make viruses
        Virus virus1 = new Virus(this);
        virus1.setPosition(new Vec2(-8, 75));

        Virus virus2 = new Virus(this);
        virus2.setPosition(new Vec2(4, 125));

        // make health points
        HealthPoint healthPoint1 = new HealthPoint(this);
        healthPoint1.setPosition(new Vec2(-3, 50));

        HealthPoint healthPoint2 = new HealthPoint(this);
        healthPoint2.setPosition(new Vec2(-2, 100));

        HealthPoint healthPoint3 = new HealthPoint(this);
        healthPoint3.setPosition(new Vec2(1, 150));
    }

    public Jet getJet() {
        return jet;
    }

 */
}
